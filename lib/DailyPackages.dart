import 'package:flutter/material.dart';

class DailyPackages extends StatelessWidget {
  const DailyPackages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(' Daily Packages'),
    );
  }
}
